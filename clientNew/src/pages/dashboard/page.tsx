import useDocumentMetadata from '@hooks/useDocumentMetadata'

const DashboardPage = () => {
    useDocumentMetadata('EX - Dashboard', 'Extreme Engineering - Dashboard Page')

    return (
        <div>DashboardPage</div>
    )
}

export default DashboardPage